<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Category Update</title>
</head>
<body>
    <header>
        <%- include('partials/navbar.ejs') %>
    </header>

    <div class="container">
        <br>
        <div class="text-center"><h2>Update Category</h2></div>
        <br>
        <div class="text-center">
            <% if (category) { %> 
                <form action="/editCategory/<%= category.categoryId %>" method="POST" enctype="multipart/form-data">
                    <!-- Hidden field for current image -->
                    <input type="hidden" name="currentImage" value="<%= category.categoryImage %>">

                    <label for="categoryName">Category Name:</label><br>
                    <input type="text" id="categoryName" name="categoryName" value="<%= category.categoryName %>" required><br><br>

                    <label for="categoryDescription">Category Description:</label><br>
                    <textarea id="categoryDescription" name="categoryDescription" required><%= category.categoryDescription %></textarea><br><br>

                    <label for="image">Current Image:</label><br>
                    <img src="/images/<%= category.categoryImage %>" width="20%"><br><br>

                    New Image (Optional):<br>
                    <input type="file" id="image" name="image" accept="image/*"><br><br>

                    <button type="submit" class="btn btn-success">Update Category</button>
                </form>
            <% } else { %>
                <p>No category found.</p>
            <% } %>
            <a href="/adminCategories">Back to Categories</a>
        </div>
    </div>
</body>
</html>
